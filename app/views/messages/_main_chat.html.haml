- @messages.each do |messeage|
  .message
    .upper-message
      .upper-message__user-name
        = messeage.user.name
      .upper-message__date
        = messeage.created_at.strftime("%Y/%m/%d %H:%M")
    .lower-meesage
      .lower-message__content
        = messeage.content